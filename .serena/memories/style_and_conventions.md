Follow Phoenix/Elixir conventions from AGENTS.md: prefer pure functions and deterministic tests, use start_supervised!/1 in tests, avoid Process.sleep, use to_form and <.input> for forms, and follow LiveView stream patterns. For HTTP use Req (not HTTPoison/Tesla/httpc). Keep code idiomatic Elixir (predicate names end with ?, avoid String.to_atom on user input, no map-access syntax on structs).