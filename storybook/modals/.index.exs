defmodule Storybook.Modals do
  @moduledoc """
  Back-office modal primitives (`ZaqWeb.Components.BOModal`).
  """
  use PhoenixStorybook.Index

  def folder_name, do: "Modals"
  def folder_index, do: 5
end
