{
  "assignee": "",
  "blocked_by": [
    "zaq-jrg.41"
  ],
  "blocks": [
    "zaq-jrg.43"
  ],
  "created": "2026-09-19T20:05:25Z",
  "description": "Functional specifications/files: extend OAuth/runtime and nested workflow integration tests to assert effective Authorization at OpenAIStub across refresh, Actor propagation and nested Agents; add deterministic expiry HTTP evidence through existing lifecycle seams.\nTests first: old OAuth token request→refresh→delivered mutation→new token request; refresh failure never falls back globally; Alice/Bob/system workflow and nested calls assert outer/inner headers and opaque-scope isolation; stale expiry callback preserves current incarnation, matching expiry fences, expired cold start makes no request.\nBranches: OAuth success/failure/revocation, nested configured agents, workflow Person/non-Person, stale/current expiry callbacks, expired grant.\nMocking: reuse Req.Test only for external OAuth token endpoint and OpenAIStub for external LLM; all ZAQ boundaries real. No production clock hook or sleeps; document that callback injection proves fencing rather than scheduler timing.\nDocs: tests document entry-point parity; update service docs only for discovered drift.\nAction reuse: not applicable — coverage reuses Connect.refresh_grant, workflow run_agent, Executor, ServerManager and existing external-edge mocks; adds no production operation, Action or tool.",
  "id": "zaq-jrg.42",
  "labels": [],
  "parent": "zaq-jrg",
  "comments": [
    {
      "text": "Implemented-uncommitted. OAuth scenario now proves old token→refresh mutation delivery→new Authorization and refresh failure retains Person token without org fallback. Workflow matrix proves two People and system select distinct credentials across opaque run/step scopes; nested X→Y→X sends the Person credential on all three LLM calls. Expiry scenario proves stale callback survival via HTTP, matching-incarnation fencing, and expired cold resolution makes no LLM request. Executor suite 25/25 and nested suite 6/6 pass; final mix q passes. Natural timer scheduling remains covered separately; this test intentionally validates deterministic fencing callbacks. Action reuse remains not applicable. Kept open pending user review before commit/push.",
      "timestamp": "2026-09-19T20:20:49Z"
    },
    {
      "text": "Final validation blocker: mix precommit passed format/Credo (16,615 mods/funs, no issues) but the full stale suite failed only in existing Zaq.People.AuthRateLimiterPeerTest 'official increment broadcasts converge...' due ExUnit 60s timeout; 8,447 tests + 110 properties otherwise completed with 1 failure. Per user instruction, stopped without changing that unrelated peer test and without commit/push. Focused related suite remains green: 283 tests + 4 properties.",
      "timestamp": "2026-09-19T20:34:28Z"
    }
  ],
  "priority": 1,
  "status": "in_progress",
  "title": "[zaq-jrg.42] OAuth workflow and nested Agent authentication HTTP coverage",
  "type": "task",
  "updated_at": "2026-09-20T03:03:34Z"
}
