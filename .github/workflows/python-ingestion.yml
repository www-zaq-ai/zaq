name: Python Ingestion

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  python-ingestion:
    name: Test (Python 3.13)
    runs-on: ubuntu-24.04
    timeout-minutes: 45

    services:
      postgres:
        image: pgvector/pgvector:pg18
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: zaq_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      MIX_ENV: test
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/zaq_test
      PYTHON_VERSION: "3.13"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          otp-version: "28.1"
          elixir-version: "1.19.5"

      - name: Restore Mix dependencies
        uses: actions/cache@v4
        with:
          path: deps
          key: ${{ runner.os }}-${{ runner.arch }}-mix-deps-${{ hashFiles('mix.lock') }}

      - name: Install Mix dependencies
        run: mix deps.get

      - name: Fetch reviewed crawler revision
        run: mix zaq.python.fetch

      - name: Set up CPython
        uses: actions/setup-python@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Restore pip downloads
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-${{ runner.arch }}-py${{ env.PYTHON_VERSION }}-pip-${{ hashFiles('priv/python/crawler-ingest/requirements.lock', 'priv/python/crawler-ingest.revision') }}

      - name: Check the Python provisioner
        run: python${PYTHON_VERSION} -m unittest discover -s test/python -p 'test_*.py' -v

      - name: Build and validate a fresh Python environment
        run: python${PYTHON_VERSION} scripts/provision_python.py priv/python/crawler-ingest .venv

      - name: Provision PostgreSQL extensions as DBA
        run: mix run --no-start .github/scripts/provision-test-database.exs postgres

      - name: Run real Python tests
        run: mix test --only real_python
