We need to make the event registry have kind of CRUD operation on the state.

- List all events with filters (boolean)
- Update event to false

So when the trigger is deactivated we need to make this event false so trigger want be triggering any workflow.
