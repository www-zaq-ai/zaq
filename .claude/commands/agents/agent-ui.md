---
agent: shadcn-ui-builder
description: Launch the ShadCN UI builder agent for interface design and implementation
---

**Not for ZAQ back office.** ZAQ BO UI uses Phoenix LiveView, `--zaq-*` tokens, and `.zaq-*` classes.

For BO work:
1. Read [`DESIGN.md`](../../DESIGN.md)
2. Use `/design`, `/extract`, `/design-migrate`, or `/replace`

Do not use ShadCN/React patterns on `lib/zaq_web/` BO pages.
