{
  "name": "zaq-e2e",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "bootstrap": "sh -c 'cd ../.. && PORT=4002 MIX_ENV=test E2E=1 MIX_BUILD_PATH=_build/test-e2e mix assets.setup && PORT=4002 MIX_ENV=test E2E=1 MIX_BUILD_PATH=_build/test-e2e mix assets.build && PORT=4002 MIX_ENV=test E2E=1 MIX_BUILD_PATH=_build/test-e2e mix ecto.create --quiet && PORT=4002 MIX_ENV=test E2E=1 MIX_BUILD_PATH=_build/test-e2e mix ecto.migrate --quiet && PORT=4002 MIX_ENV=test E2E=1 MIX_BUILD_PATH=_build/test-e2e mix run test/support/e2e/bootstrap.exs'",
    "test": "npm run bootstrap && playwright test --project=chromium",
    "test:journeys": "npm run bootstrap && playwright test specs/",
    "test:headed": "npm run bootstrap && playwright test --headed",
    "test:storybook": "STORYBOOK_ONLY=1 playwright test --project=storybook"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
